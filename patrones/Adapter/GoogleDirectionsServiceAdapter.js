import RutaService from './RutaService';

export default class GoogleDirectionsServiceAdapter extends RutaService {

    async obtenerRutas(origin, destiny, mode, vehicle) {
        // Implementación para llamar a la API de Google Maps Directions
        // y adaptar la respuesta al formato esperado
    }
}
